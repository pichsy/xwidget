package com.pichs.xwidget.view;

/**
 * 按压状态接口
 */
public interface OnPressedStateListener {
    /**
     * 按压状态改变
     *
     * @param isPressed 是否按压
     */
    void onPressedStatusChanged(boolean isPressed);
}